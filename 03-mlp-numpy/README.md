# 03 — Vectorized MLP in numpy (hand-derived backprop)

Matrices instead of a scalar graph. Derive and implement dL/dW2, dL/db2,
dL/dW1, dL/db1 by hand — no autograd this time. Dataset:
`sklearn.datasets.load_digits()` (8x8 digits, 1797 samples, no download).

**Correctness check:** test accuracy > 90% with one hidden layer.

## Run

    venv/bin/python 03-mlp-numpy/train.py

## Results

_(fill in: architecture, hyperparameters, final test accuracy)_
